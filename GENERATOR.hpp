#ifndef HEADER_HPP
# define HEADER_HPP

class	NameClass
{
	private:

	public:

	// constructor
		NameClass(void);
		NameClass(NameClass const & copy);

	// destructor
		~NameClass(void);

	// operator
		NameClass &		operator=(NameClass const & copy);
	
	// get

	// functions

};

#endif
